from django.apps import AppConfig
class CustomEmailsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "custom_emails"
    verbose_name = "Custom Email Management"
