# services

Owns the user-facing Service domain: lifecycle, configuration, processes, revisions, environment variables, secrets, endpoints, networks, volumes, database bindings, sharing and shell state.

Its APIs are declarative and must not call Docker directly.
