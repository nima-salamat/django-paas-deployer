# auth_users

Owns OTP/login validation, JWT issuance/refresh/verification, password recovery and invitation flows.

Primary route prefix: `/auth/`.

Persistent profile/permission data belongs to `users`.
