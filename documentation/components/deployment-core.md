# deployments/core

Docker-neutral execution primitives.

The package consumes normalized Service/Revision state and converts it into execution configuration. It must not treat tenant JSON as a Docker host-policy authority.
