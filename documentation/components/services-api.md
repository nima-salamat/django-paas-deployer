# services/api

HTTP boundary for the Service domain.

Configuration endpoints update desired declarative state. Docker lifecycle calls remain in `deployments`.

Secret values are never returned by revision APIs.
