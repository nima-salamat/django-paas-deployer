# config

Django project configuration package.

- `config.settings`: settings and runtime policy wiring
- `config.urls`: HTTP routing
- `config.asgi`: ASGI/WebSocket application
- `config.wsgi`: WSGI application
- `config.celery`: Celery bootstrap

Implementation: `src/config/`.
