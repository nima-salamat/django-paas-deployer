# docs Django app

This is product code, not the repository documentation tree.

It owns document/public-asset models, APIs and related Django behavior.

Implementation: `src/docs/`. Repository docs live under root `documentation/`.
