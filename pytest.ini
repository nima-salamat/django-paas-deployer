[pytest]
testpaths = app_catalog deployments deploy services plans docs custom_emails messenger core
markers =
    deployment_integration: requires the normal Django/Celery/Docker integration environment
