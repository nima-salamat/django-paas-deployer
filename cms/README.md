# cms

## Responsibility
Wagtail CMS/admin integration and operator presentation.

## Routes
The primary admin route is configured by WAGTAIL_ADMIN_PATH.
Additional root routes: /documents/ and /pages/.
The visible /admin/ route is intentionally a decoy page; real operator administration uses the configured Wagtail/Django admin paths.
