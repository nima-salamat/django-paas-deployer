See tickets.utils.get_ticket_setting defaults. Optionally seed via SystemSetting admin.

    # ----- tickets -----
    {
        "key": "tickets.max_open_per_user",
        "default": 10,
        "value_type": "integer",
        "category": "tickets",
        "label": "Max open tickets per user",
        "description": "Maximum concurrent open tickets a user may have.",
    },
    {
        "key": "tickets.create_rate_limit",
        "default": 5,
        "value_type": "integer",
        "category": "tickets",
        "label": "Ticket create rate limit",
        "description": "Max ticket creates per window.",
    },
    {
        "key": "tickets.create_rate_window",
        "default": 3600,
        "value_type": "integer",
        "category": "tickets",
        "label": "Ticket create rate window (seconds)",
        "description": "Window for create rate limit.",
    },
    {
        "key": "tickets.message_rate_limit",
        "default": 20,
        "value_type": "integer",
        "category": "tickets",
        "label": "Message rate limit",
        "description": "Max messages per window per user.",
    },
    {
        "key": "tickets.message_rate_window",
        "default": 3600,
        "value_type": "integer",
        "category": "tickets",
        "label": "Message rate window (seconds)",
        "description": "Window for message rate limit.",
    },
    {
        "key": "tickets.max_attachment_size",
        "default": 5242880,
        "value_type": "integer",
        "category": "tickets",
        "label": "Max attachment size (bytes)",
        "description": "Default 5MB.",
    },
    {
        "key": "tickets.max_attachments_per_message",
        "default": 5,
        "value_type": "integer",
        "category": "tickets",
        "label": "Max attachments per message",
        "description": "Max files per message.",
    },
