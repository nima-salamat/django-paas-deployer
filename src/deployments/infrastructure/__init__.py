"""Infrastructure adapters for deployment application use cases.

Adapters are imported from their concrete modules so importing the framework-
neutral application package never initializes Django models as a side effect.
"""
