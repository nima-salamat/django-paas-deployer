id = "synapse-mysql-mariadb"
deprecated = true
name = "Synapse + MySQL/MariaDB"
version = "1.159.0"
description = "Unavailable: upstream Synapse currently supports SQLite and PostgreSQL backends, not MySQL/MariaDB."
category = "communication"
[links]
website = "https://element.io/server"
repository = "https://github.com/element-hq/synapse"

[variants.mysql_mariadb]
label = "Synapse + MySQL/MariaDB"
availability = "unsupported"
unavailable_reason = "Upstream Synapse currently accepts sqlite3 or psycopg2 database backends; MySQL/MariaDB support is not available."

[[variants.mysql_mariadb.fields]]
id = "database"
type = "choice"
label = "Database"
default = "mysql"
options = ["mysql", "mariadb"]
